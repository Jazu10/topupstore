const validVideo = (title, classe, url, subject, thumbnail, description) => {
   if (!title || !classe || !url || !subject || !thumbnail || !description)
      return "Please enter all the fields!";
};

export default validVideo;
